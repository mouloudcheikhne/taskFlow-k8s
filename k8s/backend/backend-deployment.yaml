apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  labels:
    app: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend-container
          image: mouloudissilayine/taskflowapp:1.0
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: backend-config
